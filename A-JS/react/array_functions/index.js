let fruits = ['Apple','Orange','Avocado']
let fruitsList = fruits.map( f =>  (f + ' is awsomes! ').toUpperCase() );
// document.write(fruitsList);

let list = 
    [
        {name:'john', age:44},
        {name:'lara', age:45},
        {name:'max', age:46},
        {name:'anna', age:47},
    ]

// list.forEach(function ( arr ) {
//     console.log(arr.name.toUpperCase());
// });

// for(let i = 0; i < list.length; i++){
//     console.log(list[i].name.toUpperCase());
// };

// let list2 = list.map(function(arr){
//     console.log(arr.name.toUpperCase());
// })

// let list2 = list.map(function(arr){
//     return (arr.name.toUpperCase());
// })
// console.log(list2); // return an array

